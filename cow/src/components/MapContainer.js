import React from "react";
import Map from "./Map";
import WarsList from "./WarsList";

const MapContainer = () => {
  return (
    <div>
      <WarsList />
      <Map />
    </div>
  );
};

export default MapContainer;
